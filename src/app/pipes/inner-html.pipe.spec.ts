import { InnerHTMLPipe } from './inner-html.pipe';

describe('InnerHTMLPipe', () => {
  it('create an instance', () => {
    const pipe = new InnerHTMLPipe();
    expect(pipe).toBeTruthy();
  });
});
